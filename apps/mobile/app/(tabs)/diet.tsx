// Diet tab — same screen as the standalone diet-log route, surfaced as a main
// tab so the mobile journey matches the web tenant-app (Home / Workouts / Diet
// / My Plan / Profile).
export { default } from '../diet-log';
